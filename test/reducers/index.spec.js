import {List, Map, fromJS} from 'immutable';
import {expect} from 'chai';

import reducer from '../../src/reducers';

describe('reducer', () => {

    it('handles BASIC_ACTION', () => {
       expect(true).to.equal(true);
    });

});